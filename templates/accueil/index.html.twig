{% extends 'base.html.twig' %}
{% block title %}Les Expositions{% endblock %}
{% block body %}
{{ dump(bienvenue) }}

<div class="global">
    <asside class="contain-gallery">
    <h2>Nos Espaces</h2>
        <ul>
            {% for galerie in galeries %}
                <li><a href="{{ path('app_galerie',{'id':galerie.id}) }}" class="lien">{{ galerie.nom|upper }}</a></li>
            {% endfor %}
        </ul>
    </asside>
</div>

{% endblock %}
