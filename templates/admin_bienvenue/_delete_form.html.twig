<form method="post" action="{{ path('app_admin_bienvenue_delete', {'id': bienvenue.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bienvenue.id) }}">
    <button class="btn">Supprimer</button>
</form>
