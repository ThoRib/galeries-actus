<form method="post" action="{{ path('app_admin_galerie_delete', {'id': galerie.id}) }}" onsubmit="return confirm('Etes vous certain de vouloir supprimer cette entrée ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ galerie.id) }}">
    <button>Supprimer</button>
</form>
