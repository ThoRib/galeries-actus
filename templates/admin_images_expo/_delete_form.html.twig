<form method="post" action="{{ path('app_admin_images_expo_delete', {'id': images_expo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ images_expo.id) }}">
    <button class="btn">Supprimer</button>
</form>
