{% extends 'base_admin.html.twig' %}

{% block title %}ImagesExpo{% endblock %}

{% block body %}
    <h1>ImagesExpo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ images_expo.id }}</td>
            </tr>
            <tr>
                <th>ImageName</th>
                <td>{{ images_expo.imageName }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ images_expo.description }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ images_expo.titre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_images_expo_index') }}">back to list</a>

    <a href="{{ path('app_admin_images_expo_edit', {'id': images_expo.id}) }}">edit</a>

    {{ include('admin_images_expo/_delete_form.html.twig') }}
{% endblock %}
