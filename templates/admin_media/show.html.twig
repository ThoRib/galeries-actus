{% extends 'base.html.twig' %}

{% block title %}{{ medium.nom }}{% endblock %}

{% block body %}
<div class="admin-wrapper">
    <h1>{{ medium.nom }}</h1>
    <a href="{{ path('app_admin_media_index') }}">Retour à la liste</a>
    <a href="{{ path('app_admin_media_edit', {'id': medium.id}) }}">Editer</a>
    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ medium.id }}</td>
            </tr> #}
            {# <tr>
                <th>Nom</th>
                <td>{{ medium.nom }}</td>
            </tr> #}
            <tr>
                <th>Description</th>
                <td>{{ medium.description }}</td>
            </tr>
            <tr>
                <th>Actif</th>
                <td>{{ medium.actif ? 'Oui' : 'Non' }}</td>
            </tr>
        </tbody>
    </table>

    {{ include('admin_media/_delete_form.html.twig') }}
</div>
{% endblock %}
