{% extends 'base.html.twig' %}

{% block title %}TypeEvenement{% endblock %}

{% block body %}
    <h1>TypeEvenement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_evenement.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ type_evenement.type }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ type_evenement.description }}</td>
            </tr>
            <tr>
                <th>Source</th>
                <td>{{ type_evenement.source }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_type_evenement_index') }}">back to list</a>

    <a href="{{ path('app_admin_type_evenement_edit', {'id': type_evenement.id}) }}">edit</a>

    {{ include('admin_type_evenement/_delete_form.html.twig') }}
{% endblock %}
